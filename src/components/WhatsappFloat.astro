---
// src/components/WhatsappFloat.astro
import { Icon } from 'astro-icon/components';

const numero = '5598991304638';
const mensagem = 'Olá! Vim pelo site da ONI Tactical e gostaria de mais informações.';
const link = `https://wa.me/${numero}?text=${encodeURIComponent(mensagem)}`;
---

<a href={link} target="_blank" rel="noopener noreferrer" class="whatsapp-float" aria-label="Falar no WhatsApp">
    <Icon name="simple-icons:whatsapp" width={28} height={28} />
</a>

<style>
  .whatsapp-float {
    position: fixed;
    bottom: 80px;
    right: 24px;
    z-index: 150;
    width: 58px;
    height: 58px;
    border-radius: 50%;
    background: #25D366;
    color: #0A0A0B;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.4);
    transition: transform 0.25s, background 0.25s;
  }

  .whatsapp-float:hover {
    background: #2FE377;
    transform: scale(1.08);
  }

  @media (max-width: 480px) {
    .whatsapp-float {
      bottom: 70px;
      right: 18px;
      width: 52px;
      height: 52px;
    }
  }
</style>